<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qnaReply">

	<insert id="qnaReplyInsert" parameterType="qnaReplyVO">
		insert into qna_reply
		(member_no, qna_id, content)
		values(#{memberNo}, #{qnaId}, #{content});
	</insert>
	
	<select id="qnaReplyList" resultType="qnaReplyVO" parameterType="Integer">
		select * from qna_reply where qna_id = #{qnaId} order by `date`
	</select>

</mapper>











