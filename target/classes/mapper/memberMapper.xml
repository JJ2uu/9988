<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id="searchId" parameterType="String" resultType="memberVO">
		select * from member where id = #{id}
	</select>
	
	<select id="searchNick" parameterType="String" resultType="memberVO">
		select nickname from member where nickname = #{nickname}
	</select>
	
	<insert id="signUp" parameterType="memberVO">
		insert into member values (null, #{id}, #{pw}, #{name}, #{birth}, #{gender}, #{email}, #{tel}, #{nickname}, null, current_timestamp, 1, #{sessionId}, #{sessionLimit})
	</insert>
	
	<select id="signIn" parameterType="memberVO" resultType="memberVO">
		select * from member where id = #{id} and pw = #{pw}
	</select>
	
	<update id="keepLogin">
		update member set session_id = #{sessionId}, session_limit = #{sessionLimit} where id = #{id}
	</update>
	
	<select id="checkSession" resultType="memberVO">
		select * from member where session_id = #{sessionId}
	</select>
	
	<select id="searchEmail" parameterType="String" resultType="memberVO">
		select * from member where email = #{email}
	</select>
	
</mapper>











